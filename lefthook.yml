pre-push:
  parallel: true
  commands:
    packages-audit:
      tags: security
      run: yarn audit
    tests:
      run: task lint test:unit
      env:
        CI: true

pre-commit:
  parallel: true
  commands:
    format:
      run: task format && git add -A

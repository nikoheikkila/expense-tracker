pre-push:
  parallel: true
  commands:
    packages-audit:
      tags: security
      run: yarn audit
    tests:
      run: CI=true task lint backend:test:unit

pre-commit:
  parallel: true
  commands:
    format:
      run: task format

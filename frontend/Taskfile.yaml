version: '3'

tasks:
  typescript:
    dir: frontend
    sources:
      - ./src/**/*.ts
      - ./src/**/*.tsx
    cmds:
      - npx tsc

  dev:
    desc: Start frontend in development mode
    dir: frontend
    cmds:
      - npx vite

  build:
    desc: Build frontend for production
    dir: frontend
    deps:
      - typescript
    cmds:
      - npx vite build

  test:
    desc: Run frontend unit tests
    dir: frontend
    cmds:
      - npx vitest -c vitest.frontend.ts --dir tests {{.CLI_ARGS}}

  test:watch:
    desc: Watch for frontend unit tests
    dir: frontend
    cmds:
      - npx vitest watch {{.CLI_ARGS}}
